
import 'mocha';
// Assertion library.
import {  expect } from 'chai';
// Standalone test spies, stubs and mocks.


describe('Analysis', () => {

    it('should make the analysis', () => {
        expect(2).to.equal(2);
    });

});
